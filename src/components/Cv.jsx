export default function Cv() {
  return (
    <section
      style={{
        width: "60%",
        backgroundColor: "#fff",
        height: "900px",
        border: "1px solid var(--color-white-darken1)",
        padding: "24px",
        borderRadius: "var(--border-radius-md)",
        boxShadow: "var(--box-shadow-black-sm)",
      }}
    >
      I am the resumee
    </section>
  );
}
